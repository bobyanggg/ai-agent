# Brave Search API key (https://api.search.brave.com/) — used if YOUTUBE_API_KEY not set
BRAVE_API_KEY=xxx

# Optional: YouTube Data API v3 key — set this to get videos from the correct channel
# Create at https://console.cloud.google.com/ (enable YouTube Data API v3)
YOUTUBE_API_KEY=xxx

# Google Gemini API key
GEMINI_API_KEY=xxx

# Telegram Bot token from @BotFather
TELEGRAM_BOT_TOKEN=xxx

# Telegram chat ID (message your bot, then call getUpdates or use @userinfobot)
TELEGRAM_CHAT_ID=xxx


# YouTube channel handles or names, comma-separated (e.g. @MKBHD,@ChannelName or "Channel Name")
YOUTUBE_CHANNELS=xxx

# Optional: Gemini model (default: gemini-1.5-flash)
GEMINI_MODEL=xxx

# When YouTube has no captions, transcribe from audio with Whisper (like NoteGPT). Requires ffmpeg + pip install yt-dlp openai-whisper
TRANSCRIPT_FALLBACK=whisper
